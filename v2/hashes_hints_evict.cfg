\* The block server pads its serialization and the requester penalizes
\* size mismatches before verifying the authorizing data commitment,
\* against the draft's MUST NOT. PenaltyImpliesLie is expected to be
\* VIOLATED: the padding block server frames the honest hint server - the
\* draft's "eviction primitive rather than a defense", reproduced.
SPECIFICATION Spec
CONSTANT ByzHints = FALSE
CONSTANT ByzBlocks = TRUE
CONSTANT PenalizeSizeUnverified = TRUE
CHECK_DEADLOCK FALSE
INVARIANT TypeOK
INVARIANT PenaltyImpliesLie
