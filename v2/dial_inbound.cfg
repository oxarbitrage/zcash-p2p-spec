\* Symmetric policy: each node accepts the inbound duplicate and closes its
\* own dial instead. EventuallyOneConnection is expected to be VIOLATED: under
\* simultaneous dials each node keeps the connection the other closes, both
\* connections die, both nodes redial, and adversarial timing repeats the
\* race forever. AtMostOnePerView still holds - the flap lives entirely in
\* the gap the draft's one-sentence rule leaves open.
SPECIFICATION Spec
CONSTANT Peers = {"a", "b"}
CONSTANT Lower = "a"
CONSTANT Policy = "prefer_inbound"
CHECK_DEADLOCK FALSE
PROPERTY EventuallyOneConnection
INVARIANT TypeOK
INVARIANT AtMostOnePerView
