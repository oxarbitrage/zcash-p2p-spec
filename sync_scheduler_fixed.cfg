SPECIFICATION Spec

\* Same degraded peer set as the buggy config: p_tip holds both blocks,
\* p_lag holds only block 1.
CONSTANT Peers = {"p_tip", "p_lag"}
CONSTANT MaxBlock = 2
CONSTANT LaggingPeers = {"p_lag"}
CONSTANT LagTip = 1
CONSTANT MaxRetries = 2

\* Zebra post-fix behaviour (zebra#10679): inventory is marked missing only on
\* an explicit notfound, and notfound re-queues the block with bounded retries.
CONSTANT BuggyRouting = FALSE
CONSTANT BuggyDrop = FALSE

CHECK_DEADLOCK FALSE

INVARIANT TypeOK
INVARIANT RegistryHonest
INVARIANT RegistryHasIsSound
INVARIANT VerifiedAreReal

PROPERTY EventuallyAllVerified
